---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- RunningHub 渠道配置：个人配置和管理后台新增 RunningHub 协议，接口地址可切换国际站与国内站，只填域名也能请求 `/openapi/v2`。拉取模型返回带 `/video`、`/image`、`/tts` 后缀的家族列表，渠道测试只提示、不生成。OpenAI 协议的“RunningHub LLM”预设可正常对话。未登录直连选择 RunningHub 模型时提示先登录。
- RunningHub 视频：登录后在创作台、画布和 Agent 中分别验证纯文本、首帧、首尾帧、参考图/视频/音频生成。调用日志中的端点与参数应符合所选家族，任务轮询、成功、失败、取消和超时提示正确。切换家族或参考模式后，清晰度、比例、时长和生成音频选项随之变化；不支持的模式、尾帧缺少首帧、素材超量在提交前提示。无首尾帧端点的家族（如 `rhart-video-v3.1-lite/video`）首帧并入参考图生成。
- RunningHub 图片与语音：验证文生图、参考图编辑、批量张数和画布图片任务，本地参考图先上传到 RunningHub 再生成。TTS 验证下拉音色（如 `qwen3-tts-flash/tts`）、填写音色 ID（如 `minimax/speech-2.6-hd/tts`）、语速和画布音频任务。长任务应等待至完成或渠道超时，失败时退还算力点。
- RunningHub 工具与音乐：视频创作台和画布用参考视频验证视频编辑、延长、超分、去字幕，动作控制同时连接首帧；缺少必需素材时提示“该模型需要参考视频/首帧”。图片放大家族需要参考图。画布音频节点验证 `suno-v5/single/music` 文生音乐，以及 `minimax/music-cover/music` 连接一个参考音频节点后翻唱。
- RunningHub 真实接口：`RUNNINGHUB_LIVE=1` 验收已通过 7 个最低价请求，覆盖文生图、上传参考图编辑、vidu 首尾帧、grok 文生视频、去字幕视频工具、speech-02-turbo 语音和 suno-v5 音乐，总耗时约 150 秒；RunningHub 回报的第三方消耗合计约 0.35，语音未回报。Suno 单次返回两段 MP3 和两张封面图，语音与音乐只取第一个音频结果；去字幕结果为 `.mov`，需确认浏览器能播放。目录与参数映射依据官方 ComfyUI_RH_OpenAPI 注册表生成；`go test ./...` 与 RunningHub、渠道协议、MiniMax、视频相关前端 node 测试 22 项通过，TypeScript 检查仅剩改动前已有的 8 个错误。未运行前端构建，界面仍待验收。

- MiniMax 国内站与水印：个人配置和管理后台的 MiniMax 渠道可在接口地址下方切换国际站 `https://api.minimax.io` 与国内站 `https://api.minimax.cn`，API Key 需与站点对应。视频设置开启“添加水印”时请求携带 `aigc_watermark: true`，关闭时不携带；确认创作台、画布和历史回填一致。
- MiniMax 查询间隔：创作台、画布和后端轮询中的 MiniMax 任务约 10 秒查询一次；其他协议的视频、图片和音频轮询仍为 5 秒。
- AI 优化提示词：选择 MiniMax 官方渠道的 MiniMax-H3 后，创作台两种布局和画布视频节点的提示词面板出现“AI 优化”。分别验证纯文本、首尾帧、参考素材输入，确认弹窗可替换或保留提示词，失败、超时均有提示；未登录直连和登录代理均可用，H3-Max 与非官方渠道不显示。登录后为 `MiniMax-H3-Context-IR` 设置算力点时按次扣点。
- 2K 重生成：MiniMax-H3 768P 成功记录和画布视频节点出现“2K 重生成”，结果为新的 2K 记录或新节点，提交原提示词、首尾帧或参考素材及成片地址。本地未同步成片提示先同步到云端，失败后重试仍走重生成；确认 `MiniMax-H3-Regenerate-2K` 算力点扣点与失败退款。本地无记录、从账号任务列表恢复的历史不含原素材，只提交提示词和成片。
- MiniMax 真实接口：已用国际站 Key 确认鉴权、参数错误信封，以及 Context-IR、2K 重生成请求结构通过官方参数校验（返回余额不足而非参数错误）。账户余额不足，完整生成、查询成功、优化结果与 2K 成片仍待充值后验收。
- 本轮已运行 `go test ./...` 与 MiniMax 相关前端 node 测试，改动文件无新增 TypeScript 类型错误；同时移除 `service/gemini.go` 未使用导入，修复后端编译失败。未运行前端构建和界面验收。

- AI 链路去重：图片、视频、音频与 Agent 共用渠道地址、鉴权、代理判定和账号刷新。验证未登录直连使用所选渠道 Key，登录个人渠道和云端渠道携带各自标识，未登录云端请求报错，图片编辑／通用视频的 FormData 未被强制设置 JSON 请求头。
- 请求与任务精简：移除不会发送的前端本地调用日志及专用摘要、重复的视频创建轮询包装；创建函数直接返回任务，创作台和画布继续使用现有轮询、错误展示、保存与删除流程。后端账号调用日志保留，普通图片批量、JSON／SSE／Responses 图片解析和文本、通用音频仍需回归。
- 视频配置统一：配置面板、创作台、画布、Agent 校验及历史回填共用视频参数规则；验证模型切换后合法参数写回状态，Gemini 高分辨率／参考生成的 8 秒限制、Seedance 智能时长和 2.5 的 30 秒上限、CogVideoX 离散时长、Agnes 分辨率和时长，以及通用模型默认值。已有不支持的素材保留并交由请求校验提示。
- 模块合并：Gemini 视频和 TTS 工具、媒体引用类型、画布配置构造、提示词库按钮和 MiniMax 视频响应处理已合并；后端改为直接协议分发，需确认 Gemini／MiMo 优先级、Agnes 查询地址及 MiniMax 渠道测试行为不变。提示词库和视频设置在浅色／深色主题下待验收。
- 本次去重已补充共享请求、任务参数、图片解析、音频／Agent 及画布配置回归用例；仅检查引用与差异，未运行测试、语法检查、构建或真实上游接口。TODO 已核对，无本次对应条目需要迁移。

- 渠道协议清理：个人配置和管理后台仅提供 OpenAI、Gemini、MiniMax、MiMo；移除 `/api/ai/direct-request`、`/api/v1/tts/voices` 及旧平台的上传转换、轮询、Kling 工作台、Grok 音色设置和首页/README 推广内容。确认旧平台名称、域名和模型前缀不再触发专用适配，剩余渠道仍可使用通用模型。
- MiniMax 官方接入：默认地址、Bearer 鉴权和固定 H3 / H3-Max 模型列表正确；渠道测试仅提示，不发起收费生成。分别通过未登录浏览器直连、登录后的个人渠道代理、云端渠道验证创建、轮询、成功、失败、取消、鉴权失败及成功无视频地址，检查错误展示与媒体保存。
- MiniMax 参数与素材：配置面板、视频创作台、画布和 Agent 入口一致；切换模型后分辨率、时长均合法，默认 768P / 5 秒，文生默认 16:9，首尾帧 adaptive。验证时长/分辨率边界、尾帧缺少首帧、模式混用、Max 携带普通参考素材、超量素材、已知元信息不合规和超过 64MB 请求；H3 单独参考音频可提交，Max 不静默丢弃已有素材。
- 通用能力回归：OpenAI、Gemini、MiMo，以及 Seedance、CogVideoX、Agnes 的已有生成流程；普通生图、批量结果与多结果展示、通用音频参数、历史回填、本地保存/账号同步和浅色/深色界面。画布图片任务只保留单一结果地址，移除 KIE 图层自动拆节点与连线。
- H3 内置技能：新初始化数据使用官方渠道规则和 H3-Max 限制，保留提示词模板；尾帧需搭配首帧，Agent 沿用当前模型选择规则，不自动切换模型，也不覆盖已入库技能。
- 本轮已补充协议与 MiniMax 回归用例并检查代码引用及差异，按约定未运行语法检查、测试、构建或真实官方接口；以上交互与生成结果仍待人工验收。

- 本地前端联调：根目录 `pnpm install`、`pnpm dev`，通过 `web/.env.development.local` 连接远端后端；确认 localhost 页面、静态资源、热更新与 `/api/health` 正常，登录后配置和画布读取、素材上传、视频 Range 请求及流式响应正常，后端不可达时返回 `502`。未登录保留本地保存，登录且同步可用时读写远端账号数据。

- 配置与用户偏好：按“生图、视频、音频、文本”展开共享渠道，右侧编辑参数；浏览渠道和候选模型不改变默认项，明确设置默认后显示待保存状态。确认浅色、深色、窄屏、键盘操作及两侧长列表滚动正常。
- 配置草稿：切换分类保留编辑，取消、关闭、Esc 和遮罩关闭时可放弃修改；模型、存储开关、容量超限调整均在保存后生效。未登录仅保存本地，登录后先同步账号；模拟加载失败、快速关闭重开、旧账号响应迟到和同步部分失败，确认草稿保留、重试及保存后刷新正常。
- 渠道模型管理：新增共享渠道、手动增加模型、仅拉取当前渠道、错误定位及迟到结果隔离；空渠道可配置第一批模型，被默认项引用的渠道或模型不能直接删除。确认同名模型通过渠道 ID 区分，实际生成使用保存后的默认组合。
- 默认参数与存储：生图张数和现有开关、各音频模型的音色与格式参数、通用提示词、S3/R2 与 WebDAV 互斥及账号同步正常。

- JuxFlow ACS 更新已完成：健康接口、主要页面和 39 个 JS/CSS 资源可访问，Chrome 中已确认首页、画布列表的新品牌和导演台 GitHub 入口移除；完整主题、插件连接、创建与导出交互仍按下列事项验收。镜像及备份记录见 [部署说明](../overview/aliyun-acs.md)。
- JuxFlow 品牌：浏览器标题、导航、登录页无障碍标签、管理后台、画布列表、插件展示名与连接说明使用新名称；浅色、深色和窄屏布局正常。
- 画布命名与导出：各入口新建画布及默认导出文件名使用 JuxFlow，沿用现有编号规则；已有项目和自定义名称保留，导入导出与本地保存正常。
- 项目入口清理：普通页面、画布和管理后台不再显示项目 GitHub 按钮、版本号或版本说明，加载页面不再请求上游 VERSION、CHANGELOG；配置、主题切换和账户菜单正常。
- 3D 导演台：已局部移除现有打包文件中的 GitHub 按钮，打开、关闭、场景操作及截图回传正常；后续替换导演台产物时需继续保留这一修改。

- 阿里云 OSS 后台统一配置：官方 SDK 上传、流式和 Range 下载、中文文件名、删除、分页容量统计、保存后密钥脱敏及留空沿用。
- MySQL 初始化：唯一索引字段指定长度，设置表查询通过 GORM 转义保留字；生成任务及调用日志的请求/响应使用长文本，避免带参考图的请求超过 64 KiB 后保存失败。
- MySQL 大 JSON 同步：用户配置、素材、图片历史、视频/图片成果记录及工作流超过 64 KiB 时应完整保存；画布刷新及第二台设备读取正常。
- ACS 部署：单副本、独立数据库账号、私有 OSS、持久化数据卷、HTTPS 入口、原应用路由和 Pod 重建后数据保持。实际上线验证结果见部署说明。
- 运维配置：证书自动续期与受限 Secret 更新、SLS 标准输出和文件日志、可用性/重启/内存/磁盘告警及本项目备份脚本；告警接收人、专用只读 GHCR 凭证及数据库精细授权仍见 TODO。
- 云端接口验收已通过大 JSON、OSS 媒体与 Range、Pod 重建持久化；仍需第二台真实设备同步、浏览器视频拖动、真实模型生成和备份恢复演练。

- 上游同步范围：已同步 infinite-canvas 的媒体云同步、画布与 Agent 交互修复、火山方舟 Seedance 独立协议三个提交；AutoDL ComfyUI 工作流未同步，渠道列表不出现 AutoDL。
- 媒体自动云端同步：管理后台“全部素材云端同步”默认关闭，保存后对后续上传生效。开启且有可用云存储时，画布导入的音视频及生图、编辑、视频、音频生成结果自动转存并带回文件标识；同步失败提示“云端同步失败”并保留上游地址或本地文件，不影响生成成功状态；同一结果重复轮询不重复上传，关闭时保持原上传方式。
- 无引用云文件清理：撤销/重做历史保留 10 步；历史淘汰、重做记录被新操作丢弃或站内离开画布后，只清理不再被当前画布、其他画布、“我的素材”引用的云文件。批量图片切换或删除主图后，文件标识、大小和实际宽高随当前图片更新，生图工作台与创意工作流重新打开后可继续预览和引用。
- 画布与 Agent 交互：Agent/Codex 历史标题可重命名；Agent 回复中的节点 ID 显示为节点摘要并可点击定位；节点引用按输入框光标位置插入；视频节点失选后暂停；调整音频截取范围后试听回到新起点；空格或移动工具在节点输入面板上可拖动画布；Ctrl+滚轮在浮层上方缩放画布；删除对话记录改为弹窗确认；WebDAV 同步 MP4 保存为 `.mp4`；图片任务全部创建失败不再弹出额外提示；画布生成优先复用图片远程 URL。
- 火山方舟协议：个人配置与管理后台新增“火山方舟”渠道，默认 `https://ark.cn-beijing.volces.com/api/v3`，Agent Plan 使用 `/api/plan/v3`。未登录直连与登录代理分别验证创建 `/contents/generations/tasks`、轮询、成功读取 `content.video_url`、失败及 `expired` 转失败；参考图可用公网 URL 或 Base64，参考视频/音频不是公网 URL 时直接报错。OpenAI 协议下的 Seedance 模型名和 `/api/plan/v3` 地址不再自动走方舟路径，后台渠道测试仅方舟协议走 Seedance 配置检查。
- Seedance 与视频参数：普通 Seedance 2.0 可在设置面板直接输入分辨率（含 1080p、4k），创作台清晰度提供完整选项；fast/mini 与 2.5 保持原选项。方舟 Seedance 2.5 参考素材上限为 30 图、10 视频、10 音频，单个参考视频 200MB、参考视频/音频时长上限 30 秒。非 Gemini 渠道的 Veo 3.1 时长可选 4、6、8 秒。
- 已移除 `/api/media/references` 上传和读取接口、`PUBLIC_BASE_URL` 配置及 ACS ConfigMap 中的对应项；确认视频创作台与画布上传参考素材无回归。
- 本轮已补充方舟路径、鉴权、渠道测试、直连请求体与 `expired` 转失败用例。Go handler/service 测试在临时副本中去掉 `service/gemini.go` 未使用的 `net/http` 导入后通过（该编译错误在同步前已存在）；前端类型检查仅剩同步前已有的 8 个错误；`model-channel`、`video` 前端用例 11 项通过。未调用真实上游接口，未做浏览器交互验收。
